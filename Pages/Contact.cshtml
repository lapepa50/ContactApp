@page
@model ContactApp.Pages.ContactModel
@{
    ViewData["Title"] = "Contact";
}

<html>
<body>
    @{
        // Form layout provided by https://www.w3schools.com/asp/webpages_forms.asp 
            if (Request.Method == "POST")
            {
                string name = Request.Form["Name"].ToString();
                string email = Request.Form["Email"].ToString();
                string message = Request.Form["Message"].ToString();
            Contact contact = new Contact
            {
                Name = name,
                Email = email,
                Message = message
            }; 
            string contactjson = System.Text.Json.JsonSerializer.Serialize(contact);
            System.IO.File.WriteAllText(System.IO.Directory.GetCurrentDirectory() + @"\data\contacts.json", contactjson);
            <p>
                You entered: <br />
                Name: @name <br />
                Email: @email <br />
                Message: @message <br />
            </p>
        }
        else
        {
            <form method="post" action="">
                Name:<br />
                <input asp-for="Name", class="form-control" /><br />
                <span asp-validation-for="Name" class="text-danger"></span>
                Email:<br />
                <input asp-for="Email", class="form-control" /><br />
                <span asp-validation-for="Email" class="text-danger"></span>
                Message:<br />
                <input asp-for="Message", class="form-control" /><br />
                <span asp-validation-for="Message" class="text-danger"></span><br /><br />
                
                <input type="submit" value="Submit" class="submit" />
            </form>
        }
    }
</body>
</html>
